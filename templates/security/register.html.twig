{% extends 'base.html.twig' %}

{% block title %}Inscription | FootNews{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('styles/register.css') }}">
{% endblock %}

{% block body %}
<main class="formulaire-inscription">
    <section class="contact-form">
        <h2>S'inscrire</h2>
        <p>Créez un compte et profitez d'une expérience utilisateur unique !</p>

        {# 🔔 Affichage des erreurs #}
        {% if error is defined and error %}
            <div class="alert alert-danger">
                {{ error }}
            </div>
        {% endif %}

        <form method="post" action="{{ path('user_register') }}">
            <!-- Champ du pseudo -->
            <input type="text" name="username" placeholder="Nom d'utilisateur" class="input-field" required>

            <!-- Champ de l'email -->
            <input type="email" name="email" placeholder="Adresse e-mail" class="input-field" required>

            <!-- Champ du mot de passe -->
            <input type="password" name="password" placeholder="Mot de passe" class="input-field" required>

            <!-- Champ de confirmation du mot de passe -->
            <input type="password" name="confirm_password" placeholder="Confirmer le mot de passe" class="input-field" required>

            <!-- Conditions d'utilisation -->
            <div class="checkbox-container">
                <input type="checkbox" name="agree_terms" id="agree_terms" required>
                <label for="agree_terms">
                    En m'inscrivant, j'accepte les 
                    <a href="conditions" target="_blank">conditions d'utilisation</a> 
                    et la 
                    <a href="politiqueconfidentialite" target="_blank">politique de confidentialité</a>.
                </label>
            </div>

            <!-- Bouton de soumission -->
            <button type="submit" class="btn btn-primary">S'inscrire</button>
        </form>
    </section>
</main>

<script src="{{ asset('Javascript/confirmation-mdp.js') }}"></script>
{% endblock %}